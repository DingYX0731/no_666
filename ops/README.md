# ops

Unified operations command layer.

Use top-level:

```bash
python run_ops.py <subcommand> [options]
```

Subcommands:

- `server-time`
- `products`
- `ticker`
- `balance`
- `pending-count`
- `orders`
- `place-order`
- `cancel-order`
